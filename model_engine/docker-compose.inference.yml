services:
  craft-sample:
    build:
      context: .
      dockerfile: Dockerfile.inference
    image: towa-model-engine-inference:latest
    environment:
      HOME: /cache/models
      XDG_CACHE_HOME: /cache/models
      TORCH_HOME: /cache/models/torch
    volumes:
      - ./.runtime:/workspace_out
      - ./.cache/models:/cache/models
    command:
      - python3
      - /app/model_engine/scripts/run_craft_sample.py
      - --image
      - /app/model_engine/samples/images/sample_page.webp
      - --workspace
      - /workspace_out

  ocr-sample:
    build:
      context: .
      dockerfile: Dockerfile.inference
    image: towa-model-engine-inference:latest
    environment:
      HOME: /cache/models
      XDG_CACHE_HOME: /cache/models
      TORCH_HOME: /cache/models/torch
      HF_HOME: /cache/models/huggingface
      TRANSFORMERS_CACHE: /cache/models/huggingface/transformers
    volumes:
      - ./.runtime:/workspace_out
      - ./.cache/models:/cache/models
    command:
      - python3
      - /app/model_engine/scripts/run_ocr_sample.py
      - --image
      - /app/model_engine/samples/images/sample_page.webp
      - --workspace
      - /workspace_out

  translation-sample:
    build:
      context: .
      dockerfile: Dockerfile.inference
    image: towa-model-engine-inference:latest
    environment:
      TOWA_TRANSLATION_PROVIDER_API_KEY: ${TOWA_TRANSLATION_PROVIDER_API_KEY:-}
      HOME: /cache/models
      XDG_CACHE_HOME: /cache/models
      TORCH_HOME: /cache/models/torch
      HF_HOME: /cache/models/huggingface
      TRANSFORMERS_CACHE: /cache/models/huggingface/transformers
    volumes:
      - ./.runtime:/workspace_out
      - ./.cache/models:/cache/models
    command:
      - python3
      - /app/model_engine/scripts/run_translation_sample.py
      - --image
      - /app/model_engine/samples/images/sample_page.webp
      - --workspace
      - /workspace_out

  pipeline:
    build:
      context: .
      dockerfile: Dockerfile.inference
    image: towa-model-engine-inference:latest
    environment:
      TOWA_TRANSLATION_PROVIDER_API_KEY: ${TOWA_TRANSLATION_PROVIDER_API_KEY:-}
      TOWA_NANOBANANA_API_KEY: ${TOWA_NANOBANANA_API_KEY:-}
      HOME: /cache/models
      XDG_CACHE_HOME: /cache/models
      TORCH_HOME: /cache/models/torch
      HF_HOME: /cache/models/huggingface
      TRANSFORMERS_CACHE: /cache/models/huggingface/transformers
    volumes:
      - ./.runtime:/workspace_out
      - ./.cache/models:/cache/models
    command:
      - python3
      - /app/model_engine/scripts/run_pipeline_sample.py
      - --image
      - /app/model_engine/samples/images/sample_page.webp
      - --workspace
      - /workspace_out

  craft-preload:
    build:
      context: .
      dockerfile: Dockerfile.inference
    image: towa-model-engine-inference:latest
    environment:
      HOME: /cache/models
      XDG_CACHE_HOME: /cache/models
      TORCH_HOME: /cache/models/torch
      HF_HOME: /cache/models/huggingface
      TRANSFORMERS_CACHE: /cache/models/huggingface/transformers
    volumes:
      - ./.cache/models:/cache/models
    command:
      - python3
      - /app/model_engine/scripts/preload_craft.py
      - --cache-dir
      - /cache/models

  ocr-preload:
    build:
      context: .
      dockerfile: Dockerfile.inference
    image: towa-model-engine-inference:latest
    environment:
      HOME: /cache/models
      XDG_CACHE_HOME: /cache/models
      TORCH_HOME: /cache/models/torch
      HF_HOME: /cache/models/huggingface
      TRANSFORMERS_CACHE: /cache/models/huggingface/transformers
    volumes:
      - ./.cache/models:/cache/models
    command:
      - python3
      - /app/model_engine/scripts/preload_manga_ocr.py
      - --cache-dir
      - /cache/models

  inpaint-sample:
    build:
      context: .
      dockerfile: Dockerfile.inference
    image: towa-model-engine-inference:latest
    environment:
      TOWA_NANOBANANA_API_KEY: ${TOWA_NANOBANANA_API_KEY:-}
      HOME: /cache/models
      XDG_CACHE_HOME: /cache/models
      TORCH_HOME: /cache/models/torch
      HF_HOME: /cache/models/huggingface
      TRANSFORMERS_CACHE: /cache/models/huggingface/transformers
    volumes:
      - ./.runtime:/workspace_out
      - ./.cache/models:/cache/models
    command:
      - python3
      - /app/model_engine/scripts/run_inpaint_sample.py
      - --image
      - /app/model_engine/samples/images/sample_page.webp
      - --workspace
      - /workspace_out
