-r requirements-base.txt

# Local inference dependencies for built-in text detection.
# CRAFT/OpenCV dependency chain still expects the NumPy 1.x ABI.
numpy<2
torch
torchvision
opencv-python-headless
craft-text-detector
